new Array(22 * 8).fill().forEach(() => {
    const input = document.createElement('input');
    input.setAttribute('type', 'text');
    input.className = 'w-full h-full outline-none text-3xl text-center col-span-1 row-span-1 shadow-inner bg-white';
    // document.getElementById('scorecard').append(input);
})